# Impeccable: machine-local overrides and ephemeral review/build artifacts.
.impeccable/config.local.json
.impeccable/review/
.impeccable/build/
.impeccable/mocks/

# sdlc: local lock and session state.
.sdlc/

# Claude Code: machine-local session state (settings.json is committed).
.claude/*
!.claude/settings.json
