export function DemoNote() {
  return (
    <p className="border-t border-hairline px-6 py-3 text-[12px] text-ink-faint">
      Demo data — names, amounts and statuses on this screen are synthetic and do not
      represent real Clients, Agents or invoices.
    </p>
  );
}
